spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.fstpay: DEBUG
    org.springframework.security: INFO

server:
  error:
    include-message: always
    include-binding-errors: always
